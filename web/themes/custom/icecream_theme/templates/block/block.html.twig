{#
  Simple block wrapper for icecream_theme.
#}
<div{{ attributes.addClass('block', 'block--simple') }}>
  {% if label %}
    <h2 class="block__title">{{ label }}</h2>
  {% endif %}

  <div class="block__content">
    {{ content }}
  </div>
</div>
